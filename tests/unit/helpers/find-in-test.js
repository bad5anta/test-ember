import { findIn } from 'lits-test/helpers/find-in';
import { module, test } from 'qunit';

module('Unit | Helper | find in');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = findIn([42]);
  assert.ok(result);
});
