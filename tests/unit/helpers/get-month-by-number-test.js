import { getMonthByNumber } from 'lits-test/helpers/get-month-by-number';
import { module, test } from 'qunit';

module('Unit | Helper | get month by number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getMonthByNumber([42]);
  assert.ok(result);
});
